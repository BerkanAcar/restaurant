<p>
	<strong>Kategori:</strong>
	<%= @place.category.title %>
</p>
<p>
  <strong>Name:</strong>
  <%= @place.name %>
</p>
 
<p>
  <strong>Description:</strong>
  <%= @place.description %>
</p>
<%= render 'reservations/form' %>
<% if current_owner.present? && @place.owner_id == current_owner.id %>
	<%= link_to 'Edit', edit_place_path %>
	<%= link_to 'Delete', place_path, method: :delete %>
<% end %>
<%= link_to 'Back', places_path %></br>
<%= render 'comments/comment' %>